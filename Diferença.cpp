#include <iostream>
using namespace std;

int main() {

    int A, B, C, D;
    cin >> A >> B >> C >> D;
    int diff = (A* B) - (C * D);

    cout << "DIFERENÇA = " << diff << endl;

    return 0;
}